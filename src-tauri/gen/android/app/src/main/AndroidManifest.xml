<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Exact alarms: USE_EXACT_ALARM is attempted first (granted at install
         to eligible health/fitness habit-tracking apps); JS degrades to
         inexact scheduling if the device grants neither permission. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Notifications (Android 13+ runtime permission still required). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.app"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exact-alarm reminders: fires at the precise configured minute. -->
        <receiver
            android:name=".plugins.exactalarms.AlarmAlarmReceiver"
            android:exported="false" />
        <!-- Notification action buttons (Mark done / Snooze). -->
        <receiver
            android:name=".plugins.exactalarms.ActionReceiver"
            android:exported="false" />
        <!-- Re-applies all persisted alarms after a device reboot. -->
        <receiver
            android:name=".plugins.exactalarms.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widgets: interactive, data fed by the JS widget bridge. -->
        <receiver
            android:name=".plugins.widgetdata.TodoWidgetProvider"
            android:exported="true"
            android:label="@string/widget_todo_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_today" />
        </receiver>
        <receiver
            android:name=".plugins.widgetdata.HabitsStreaksWidgetProvider"
            android:exported="true"
            android:label="@string/widget_streaks_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_streaks" />
        </receiver>
        <receiver
            android:name=".plugins.widgetdata.HabitsWeekGridWidgetProvider"
            android:exported="true"
            android:label="@string/widget_weekgrid_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_weekgrid" />
        </receiver>
        <receiver
            android:name=".plugins.widgetdata.HabitsHeatmapWidgetProvider"
            android:exported="true"
            android:label="@string/widget_heatmap_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_heatmap" />
        </receiver>
        <receiver
            android:name=".plugins.widgetdata.HabitsMomentumWidgetProvider"
            android:exported="true"
            android:label="Momentum">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_momentum" />
        </receiver>
        <!-- Interactive widget actions (checkbox taps) — must be exported so
             the launcher can deliver fill-in intents from other processes. -->
        <receiver
            android:name=".plugins.widgetdata.WidgetActionsReceiver"
            android:exported="true" />
        <!-- Collection-adapter services for the ListView widgets. -->
        <service
            android:name=".plugins.widgetdata.TodoRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        <service
            android:name=".plugins.widgetdata.HabitsRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        <service
            android:name=".plugins.widgetdata.HabitsWeekGridRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        <service
            android:name=".plugins.widgetdata.HabitsHeatmapRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
